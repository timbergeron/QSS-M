mpg123 1.33.7, MSYS2 mingw-w64-{i686,x86_64}-mpg123-1.33.7-1 packages.

The decoder DLL and shared mpg123.h/fmt123.h headers are from those packages.
Header trailing whitespace is removed to match the existing vendored style.
These are upstream builds; no local decoder or logging patches are applied.
Only libmpg123-0.dll is bundled. Output drivers and the command-line player
are unused. The existing import libraries remain compatible.
